#! /bin/csh

setenv project hawaii

cd $PROJ_DIR/ingest/params

running "Grib2toMdv -params Grib2toMdv.gfs.hawaii"

if ($status == 1) then
    Grib2toMdv -params Grib2toMdv.gfs.hawaii -debug |& \
	LogFilter -d $ERRORS_LOG_DIR -p Grib2toMdv -i gfs.hawaii >& /dev/null &
endif

