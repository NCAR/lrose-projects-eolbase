#! /bin/csh -f

setenv PROJ_DIR ~dixon/projDir
setenv DATA_HOST eldora.eol.ucar.edu

xterm -title RadMon -bg black -fg green -g 80x3-0+0 -fn fixed -e RadMon -params $PROJ_DIR/display/params/RadMon.relay.sband -summary &
